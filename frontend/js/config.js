/**
 * config.js — runtime configuration for the frontend.
 * Change API_BASE_URL if the Go backend is not on localhost:8080.
 */
window.APP_CONFIG = {
  API_BASE_URL: 'https://ai-based-smart-governance-and-compliance-fc8y.onrender.com/',
};
